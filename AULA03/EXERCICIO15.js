function calculaPotencia(base, expoente) {
    return Math.pow(base, expoente);
  }
  
  // Exemplo de uso:
  const resultado = calculaPotencia(2, 3); // 2 elevado à 3 é 8
  console.log(resultado); // Saída: 8
  